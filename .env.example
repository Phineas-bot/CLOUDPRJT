# Copy to .env and fill secrets before running docker compose

# Core
DFS_AUTH_SECRET=change-me

# SendGrid (optional, preferred if available)
SENDGRID_API_KEY=your-sendgrid-api-key
SENDGRID_FROM_EMAIL=ngnindem0@gmail.com

# SMTP fallback (used if SendGrid is not configured or fails)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ngnindem0@gmail.com
SMTP_PASS=utab iarj xrbt pnus
SMTP_FROM_EMAIL=ngnindem0@gmail.com
SMTP_STARTTLS=1

# SMS (optional)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# Master service
DFS_MASTER_HOST=master
DFS_MASTER_PORT=50050
